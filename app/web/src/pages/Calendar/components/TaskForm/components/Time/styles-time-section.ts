import { BORDER_CLASS, TEXT_COLOR_CLASS, TEXT_SIZE_CLASS } from "../../../../../../styles/styles-class";

export const TIME_TEXT_CLASS = `${TEXT_SIZE_CLASS} ${TEXT_COLOR_CLASS} font-medium`;
export const TIME_BOX_INPUT_CLASS = `${TIME_TEXT_CLASS} flex flex-row items-center ${BORDER_CLASS} h-7 p-[5px]`;
